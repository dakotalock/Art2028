{
  "name": "react-app",
  "version": "1.0.0",
  "description": "A simple React app",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-dom/client": "^18.0.0"  // Required for React 18
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "react-scripts": "5.0.0"  // The latest compatible version with React 18
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": "22.x"  // Required for compatibility with Vercel after Node.js 16's end-of-life
  },
  "config": {
    "react": {
      "version": "18"  // Specify React version if required
    }
  }
}
